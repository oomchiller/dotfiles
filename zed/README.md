# zed

## Installation

```sh
cd ~/.config/dotfiles
stow -t ~ zed
```

This package tracks the durable Zed configuration in `~/.config/zed`:

- `settings.json`
- `keymap.json`

Runtime state such as prompt libraries and other generated databases stays outside the repo.
